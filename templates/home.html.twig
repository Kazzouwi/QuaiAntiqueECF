{% extends "base.html.twig" %}

{% block main %}

    <div class="mealDisplay">
        {% for meal in meals %}
            <div class="oneMeal">
                <h1 class="mealName">{{ meal.name }}</h1>
                <img src="{{ asset('/uploads/meal/' ~ meal.image)}}" class="mealTitle"/>
            <div>
        {% endfor %}

    </div>

{% endblock main %}