<form action="{{ path('reservation_search') }}">
    <label for="places">Rechercher tables</label>
    <input type="number" name="places" min="1" max="10" onchange="searchByPlaces(this.value);">
    <input type="submit">
</form>

<h1>Liste des tables : </h1>
{% for table in tables %}
    <h1>{{ table.number }}</h1>
{% endfor %}